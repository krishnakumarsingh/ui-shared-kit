plugins {
  id "org.sonarqube" version "3.3"
}
sonarqube {
  properties {
    property "sonar.projectKey", "beldenmuikit"
    property "sonar.organization", "beldenmuikit"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}